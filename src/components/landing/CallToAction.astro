---

---

<section
  id="contactanos"
  class="relative py-32 text-center text-white font-sans min-h-screen flex flex-col justify-center"
>
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img
      src="/images/lluvioso.png"
      alt="Call to Action Background"
      class="w-full h-full object-cover"
    />
    <!-- Overlay for better text readability -->
    <div class="absolute inset-0 bg-slate-900/60"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-4xl mx-auto px-4 animate-fade-in-up">
    <h2
      class="text-4xl md:text-6xl font-bold mb-6 tracking-tight leading-tight drop-shadow-md"
    >
      ¡Encuentra la casa de tus sueños hoy mismo!
    </h2>
    <p
      class="text-xl text-slate-100 mb-10 max-w-2xl mx-auto font-light leading-relaxed drop-shadow-sm"
    >
      No dejes pasar la oportunidad de vivir en el lugar que siempre imaginaste.
      Contáctanos y hazlo realidad.
    </p>

    <a
      href="#contact"
      class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white transition-all duration-300 bg-emerald-600 rounded-full hover:bg-emerald-700 hover:scale-105 shadow-lg hover:shadow-emerald-500/30 group"
    >
      Contáctanos
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="w-5 h-5 ml-2 transition-transform duration-300 group-hover:translate-x-1"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
      </svg>
    </a>
  </div>
</section>

<style>
  @keyframes bounce-in-up {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    60% {
      opacity: 1;
      transform: translateY(-5px);
    }
    100% {
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: bounce-in-up 1s ease-out forwards;
  }
</style>
