<section id="inicio" class="hero min-h-screen relative font-sans">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img
      src="/images/image2.jpeg"
      alt="Luxury Home"
      class="w-full h-full object-cover"
    />
    <!-- Subtle overlay to ensure text readability -->
    <div class="absolute inset-0 bg-slate-900/20"></div>
  </div>
  <div
    class="hero-content text-center text-neutral-content z-10 w-full flex-col pt-50"
  >
    <div class="max-w-3xl animate-fade-in-up">
      <h1
        class="mb-5 text-5xl md:text-7xl font-bold text-white tracking-tight drop-shadow-md leading-tight"
      >
        Tu espacio ideal, <span class="text-emerald-400">200m²</span> de oportunidad
        en Ciudad de Dios
      </h1>
      <p
        class="mb-8 text-lg font-normal text-white/95 max-w-3xl mx-auto drop-shadow-md leading-relaxed"
      >
        Ubicación estratégica en Yura a solo 3 cuadras de la Panamericana.
        Servicios completos, módulo habitable y la tranquilidad que tu familia
        merece. ¡Listo para traspaso inmediato!
      </p>
    </div>

    <div class="w-full flex justify-center mt-8 animate-bounce">
      <a
        href="#inmueble"
        class="btn btn-circle bg-emerald-600 hover:bg-emerald-700 text-white border-none shadow-lg hover:shadow-xl transition-all"
        aria-label="Ver más detalles"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-8 w-8"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div>
  </div>

  <!-- Content overlay (houses) in the bottom part of the image, handled by the image itself,
       but if we needed to overlay content we would do it here -->
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes bounce-in-up {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    60% {
      opacity: 1;
      transform: translateY(-5px);
    }
    100% {
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: bounce-in-up 1s ease-out forwards;
  }

  .shake-animation {
    animation: shake 0.5s cubic-bezier(0.36, 0.07, 0.19, 0.97) both;
  }

  @keyframes shake {
    10%,
    90% {
      transform: translate3d(-1px, 0, 0);
    }
    20%,
    80% {
      transform: translate3d(2px, 0, 0);
    }
    30%,
    50%,
    70% {
      transform: translate3d(-4px, 0, 0);
    }
    40%,
    60% {
      transform: translate3d(4px, 0, 0);
    }
  }
</style>
